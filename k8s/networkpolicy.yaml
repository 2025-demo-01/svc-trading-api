apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: trading-api-deny-by-default
  namespace: trading
spec:
  podSelector: { matchLabels: { app: svc-trading-api } }
  policyTypes: ["Ingress","Egress"]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: gateway
      ports:
        - protocol: TCP
          port: 8080
  egress:
    - to:
        - namespaceSelector: {}   # Istio sidecar/mesh
      ports:
        - protocol: TCP
          port: 15090
    # [ADDED] 예시: MSK/Aurora egress 목적지(환경에 맞춰 조정)
    # - to:
    #     - ipBlock: { cidr: 10.0.0.0/16 }  # MSK brokers CIDR
    #   ports: [{ protocol: TCP, port: 9094 }]
    # - to:
    #     - ipBlock: { cidr: 10.1.0.0/16 }  # RDS Proxy CIDR
    #   ports: [{ protocol: TCP, port: 5432 }]
